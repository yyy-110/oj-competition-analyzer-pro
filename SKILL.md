---
name: oj-competition-analyzer-pro
description: >
  OJ (Online Judge) 多班级全景竞赛分析专业版工具。专为 krand.site (HUSTOJ) 打造，支持导入 Excel 班级名单进行【全年级分析】、【分班级下钻分析】或【二者兼备的全景大屏对比】。实现线上全量数据/AC与错误代码自动抓取、班级智能映射、AST 代码异常与跨班作弊审计、AC 算法派系聚类、典型错误三维诊断，生成支持班级即时切换、班级专属真实代码样例与实名/脱敏双模的现代化交互式 HTML 教学大屏报告。
  典型触发词与场景包括："生成oj多班级报告"、"分析OJ班级竞赛"、"带班级名单分析oj"、"按班级生成OJ分析报告"、"对比各班级oj成绩与作弊情况"、"生成 OJ 竞赛大屏报告"等。
---

# OJ 竞赛多班级全景分析专业版 (krand.site Pro 版)

本 Skill 专为 `krand.site` (HUSTOJ) 教学竞赛打造，全面实现**Excel 班级名单智能解析**、**年级与各班级无缝即时下钻**、**题目通关/学情画像/代码异常/AC算法/错误诊断全模块 (1~7部分) 班级动态联动**、**班级专属真实代码样例提取**与**专属班级横向对比大屏**。

---

## 🎯 触发提示与前置询问规则 (Pre-flight Checks)

### 1. 触发词（Trigger Phrases）
当用户输入以下类似语句时，**立即触发此 skill**：
- `生成oj多班级报告` / `按班级分析oj` / `生成 OJ 班级对比报告`
- `带班级名单分析竞赛` / `生成带班级的全景oj大屏`
- `分析 OJ 竞赛提交记录并分班级对比` / `检查各班学生代码作弊与学情画像`

### 2. 触发后的必须动作（主动询问清单）
触发本 skill 后，Agent **必须首先主动向用户询问**获取以下关键参数：
1. **竞赛 CID (Contest ID)**：竞赛的数字编号（例如 `1081`）；
2. **平台 Cookie**：拥有查看权限的 Cookie 字符串（例如 `PHPSESSID=xxx`；若用户在当前会话中已经提供过 Cookie，需主动询问是否复用）；
3. **是否需要分班级分析 & 班级名单 Excel 文件路径**：
   - 询问用户：“是否需要按班级进行对比与下钻分析？若是，请提供班级名单 Excel 文件路径（`.xlsx` / `.xls`，格式要求：第一行为班级名，列下方为学生姓名）”；
   - 若用户提供名单，启用【全年级 + 班级全景下钻模式】；若用户明确不分班，则按【全年级宏观模式】输出；
4. **基础题免检范围（可选，推荐默认 A-J）**：
   - 询问用户：“是否指定免检基础题范围？（推荐默认：A-J，这些基础题将跳过雷同与超纲分析，第六/七部分仅分析非基础进阶题）”；
5. **深度聚类分析题目范围（可选，推荐默认 all）**：
   - 询问用户：“是否指定深度算法与错误聚类分析的题目？（推荐默认：all，将自动分析所有非基础题目）”。

---

## 📋 永久固化报告 6 大核心排版与全模块动态联动标准

生成最终 HTML 报告时，**必须严格遵循以下 6 项标准**：

1. **真实用户名提取与脱敏机制**：
   - 严禁提取或展示学生乱填的自定义昵称 `nick`（如“洗洁精”、“风鱼柳音”等），**全程统一提取和展示真实用户名 `user`**（如“许珺杰2028”、“胡笑遇2028”）；
   - 脱敏模式下统一映射为全局代号（如 `Student_001`）。
2. **第二部分（25 题难度与通关分布）班级动态联动与自适应评级**：
   - 各题尝试人数、通关人数、总提交、AC 提交按当前选定班级实时更新；
   - **学生通关比例**分母采用该班有效参赛人数（`total_with_subs`），进度条精准自适应；
   - **难度等级自适应动态重评**：根据当前选定班级的实际通关表现动态判定（简单/中等/困难/极难），直观暴露班级间的难度体感差异。
3. **第三部分（困难学生画像）班级动态联动与严格 8 列表头**：
   - 表头严格为：**`序号`、`账号`、`总提交`、`AC 提交`、`通过题数`、`个人 AC 率 (可视化)`、`试错/失败数`、`学习诊断画像`**；
   - 切换班级时**仅展示当前班级的困难学生**，班内序号从 1 重新自增排序，顶部提示文本实时联动显示该班困难生数量，无困难生时展示友好绿色表扬提示。
4. **第四部分（代码异常与作弊审计）班级动态联动与 4 小节结构**：
   - **4.1 非基础题首次即 AC**：全年级展示全校事实说明与最接近学生；具体班级视角展示该班核查事实（0人）；
   - **4.2 极速连交/秒交**：切换班级时**仅展示当前班级学生的连交记录**，无记录展示友好绿色提示；
   - **4.3 雷同代码深度聚焦（除基础题）**：**仅展示涉及当前班级学生的雷同题目与雷同群组**，保持与参考报告一致的简洁排版；仅当本班涉及固定作弊团伙时展示警告卡片；
   - **4.4 违规语言与 AI 自曝注释**：切换班级时左侧仅展示本班 C/C++ 违规学生，右侧仅展示本班 AI 注释学生。
5. **第六部分（AC 算法精细剖析）与第七部分（典型错误代码诊断）**：
   - **第六部分**：排除基础题，切换班级时**自动隐藏 0 人流派**，**代码样例 100% 提取展示当前班级学生编写的真实 AC 代码**；
   - **第七部分**：Top 10 核心题，切换班级时**自动隐藏 0 命中模式**，**错误代码与提交 ID 100% 提取展示当前班级命中学生的真实代码与提交编号**，保留三维逐段诊断。
6. **精简 7 个主 Tab 导航 + 上方独立切换大屏**：
   - 下方主 Tab 导航栏严格只有 **7 个分析模块**（一至七部分，全模块支持班级动态联动）；
   - 【📊 班级横向对比大屏】仅在上方控制栏独立切换展示（展示各班关键指标矩阵表与 25 题攻关率热力全景表）。

---

## 🚀 完整执行三步工作流

一旦获取到用户参数，严格按以下步骤依次执行：

### 第一步：全量数据抓取与源码自动下载

运行 `scripts/fetch_data.py`：

```bash
python3 /Users/yanyingying/.gemini/config/skills/oj-competition-analyzer-pro/scripts/fetch_data.py \
  --cid <CID> \
  --cookie "<COOKIE>" \
  --output-dir <输出目录> \
  --download-codes \
  --download-error-codes
```

**自动产出**：
- `prob_titles.json`：提取竞赛全称与题目中文名称；
- `rank_users.json`：全员榜单及真实用户名（剥离乱填昵称）；
- `all_subs.json`：全量提交流水日志；
- `codes/`：全部 AC 源码；
- `error-codes/`：全部错误提交代码。

---

### 第二步：代码异常挖掘与 AST 算法/错误全样本聚类

运行 `scripts/analyze_code.py`：

```bash
python3 /Users/yanyingying/.gemini/config/skills/oj-competition-analyzer-pro/scripts/analyze_code.py \
  --codes-dir <输出目录>/codes \
  --error-codes-dir <输出目录>/error-codes \
  --all-subs-file <输出目录>/all_subs.json \
  --basic-probs "<基础题范围，如 A-J>" \
  --prob-titles-file <输出目录>/prob_titles.json \
  --deep-probs "all" \
  --output <输出目录>/anomalies.json \
  --deep-output <输出目录>/deep_analysis.json
```

**自动产出**：
- `anomalies.json`：语言违规、高级语法、雷同对、固定作弊团伙与突发秒交审计；
- `deep_analysis.json`：包含每位学生真实代码映射 `student_samples` 的 AC 算法流派与错误代码聚类数据。

---

### 第三步：渲染单页多班级交互全景 HTML 大屏报告

运行 `scripts/generate_report.py`：

```bash
python3 /Users/yanyingying/.gemini/config/skills/oj-competition-analyzer-pro/scripts/generate_report.py \
  --rank <输出目录>/rank_users.json \
  --subs <输出目录>/all_subs.json \
  --anomalies <输出目录>/anomalies.json \
  --deep-analysis <输出目录>/deep_analysis.json \
  --prob-titles-file <输出目录>/prob_titles.json \
  --roster "<班级名单Excel文件路径，若有>" \
  --contest-name "<竞赛名称>" \
  --mode "both" \
  --output-dir <输出目录>
```

**自动产出**：
- 🖥️ `report.html`：单页集成式全景交互报告（纯黑代码框、班级即时切换、全模块动态联动、班级专属真实代码采样、实名/脱敏一键切换）；
- 📋 `student_anon_mapping.json`：实名与脱敏代号对照表；
- 📊 `class_summary.json`：各班级聚合指标结构化数据。
